/**
 * This file was automatically generated by Stylus and represents a Rust program.
 * For more information, please see [The Stylus SDK](https://github.com/OffchainLabs/stylus-sdk-rs).
 */

// SPDX-License-Identifier: MIT-OR-APACHE-2.0
pragma solidity ^0.8.23;

interface IPaymentRouter  {
    function init(uint256 fee_percentage) external;

    function authorizeAgent(address agent) external;

    function revokeAgent(address agent) external;

    function deposit() external payable;

    function initiatePayment(address payee, uint256 amount) external returns (uint8[32] memory);

    function completePayment(uint8[32] calldata payment_id) external;

    function withdraw(uint256 amount) external;

    function getBalance(address agent) external view returns (uint256);

    function isAuthorized(address agent) external view returns (bool);

    function getPayment(uint8[32] calldata payment_id) external view returns (address, address, uint256, uint256, bool);

    function getPaymentCount() external view returns (uint256);

    function getFeePercentage() external view returns (uint256);

    function hashId(uint256 count) external pure returns (uint8[32] memory);
}
